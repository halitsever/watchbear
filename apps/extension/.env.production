# Deployed server URL for production builds. Should be https (socket.io upgrades
# it to wss automatically). Leave unset to fall back to http://localhost:3000.
VITE_SERVER_URL=https://watchbear-server.deepfeld.com
